<!DOCTYPE html>
<html>
  <head>
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-109917857-1"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-109917857-1');
    </script>

    <title>Торговый Дом Химик</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="assets1/plugins/ionicons/css/ionicons.min.css">
    <script src='https://www.google.com/recaptcha/api.js'></script>
  </head>

  <body data-spy="scroll" data-target="#navbar_main" data-offset="80">
    <% flash.each do |key, value| %>
          <div class="flash_<%= key %>">
                <%= value.html_safe %>
          </div>
    <% end %>

    <% if Rails.env.development? %>
      <div class="flash_notice">
        cookies[:session_id] <%= cookies[:session_id] %> 
        current_session_id <%= current_user.current_session_id if current_user %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
