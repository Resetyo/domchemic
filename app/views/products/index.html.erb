<header class="catalog">
  <a href="/" data-turbolinks="false" class="btn btn-primary">Троговый Дом Химик</a>
  <a href="/lists" data-turbolinks="false" class="btn btn-primary">
    К списку
    <% hidden = "hidden-only" if @codes_list.blank? %>
    <span class="products-count <%= hidden %>"><%= @codes_list.count if @codes_list %></span>
  </a>
</header>
<h1>Каталог продукции</h1>
<p class="text-center">Минимальный чек в магазине 3000 руб.</p>
<p class="text-center"><%= link_to "Скачать прайс в Excel", products_path(format: 'xls') %></p>
<div class="table-wrapper table-catalog">
  <%= datagrid_form_for @grid, :method => :get, :url => products_path %>
  
  <%= datagrid_table @grid %>
  <%= paginate(@grid.assets) %>
</div>